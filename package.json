{
  "name": "lit-employee-app",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wds --open",
    "start": "wds --open",
    "test": "vitest",
    "test:run": "vitest --run",
    "coverage": "vitest --run --coverage"
  },
  "dependencies": {
    "@vaadin/router": "1.7.5",
    "lit": "3.1.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.1.35",
    "@web/dev-server-esbuild": "^0.2.14",
    "vitest": "^1.6.0",
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom": "^24.1.0",
    "@testing-library/dom": "^9.3.0",
    "@testing-library/user-event": "^14.5.2"
  }
}
